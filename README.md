Link condenser is an online application for shortening large URLs, makes use of base 62 (a-z,0-9,A-Z) for hashing into short urls. This also blocks spamming requests and filters using Token Bucket rate limiter. The core critical section has been bounded by Reetrant locking mechanism and also maintained a server side cache to save redundant requests.
